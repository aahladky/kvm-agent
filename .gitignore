# regenerable data / heavy blobs - never in git
runs/
logs/
scratch/
/models/
_dbg/
*.gguf
waa/cache/
waa/results/
waa/shakedown_results/

# secrets - never in git
.env.local

# vendored upstream clone: reconstructable; we vendor only the 3 used files
# under kvm_agent/_vendor/osworld/. Delete evocua/ once the vendored path is rig-verified.
evocua/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
