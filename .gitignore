# generated/runtime output — single root, see docs/PROJECT_LAYOUT.md
var/
/models/
*.gguf

# legacy scattered paths some untouched shim-era tools still emit to directly
# (live_ctl.py, tools/{operate,run_probe,eval_harness}.py — see PROJECT_LAYOUT.md
# "known exceptions"); delete this block once those tools are migrated or retired
runs/
_dbg/

# secrets - never in git
.env.local

# vendored upstream clone: reconstructable; we vendor only the 3 used files
# under kvm_agent/_vendor/osworld/. Delete evocua/ once the vendored path is rig-verified.
evocua/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
