{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then python3 \"$CLAUDE_PROJECT_DIR/tools/check_layout.py\" || { echo 'check_layout.py failed -- see docs/PROJECT_LAYOUT.md'; exit 2; }; fi"
          }
        ]
      }
    ]
  }
}
